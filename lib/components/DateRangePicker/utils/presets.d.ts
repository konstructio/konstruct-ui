import { DateRange, DateRangePreset } from '../DateRangePicker.types';
/**
 * Calculates the date range for a given preset.
 */
export declare const calculatePresetRange: (preset: DateRangePreset) => DateRange;
