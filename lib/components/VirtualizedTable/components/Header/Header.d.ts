import { RowData } from '../../VirtualizedTable.types';
import { Props } from './Header.types';
export declare const Header: <TData extends RowData>({ className, classNameArrows, classNameActiveArrows, }: Props) => import("react/jsx-runtime").JSX.Element;
